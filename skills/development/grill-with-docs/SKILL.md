---
name: grill-with-docs
description: 不留情面的面试，用来打磨一个计划或设计，同时沿途产出文档（CONTEXT.md 词汇表与 ADR）。
disable-model-invocation: true
---

调用 Skill 工具两次，分别加载 "grilling" 和 "domain-modeling"。

面试按 `grilling` 的机制推进；每当一个术语被敲定、一个难以反转的决策落地，按 `domain-modeling` 的纪律当场写入 `CONTEXT.md` / `docs/adr/`，不要攒到最后。
